<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Создание нового поста в ЗапретоГрамм</title>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center align-items-center" style="height: 100vh;">
      <div class="col-lg-6">
        <%= form_with(model: @post, html: { class: "justify-content-center" }) do |form| %>
      <div class="field row justify-content-center align-items-center">
          <%= form.label :images %>
          <%= form.file_field :images, multiple: true %>
      </div>

      <div class="field row justify-content-center align-items-center mb-4">
        <%= form.label :body %>
        <%= form.text_area :body %>
      </div>

      <div class="actions row justify-content-center align-items-center">
        <%= form.submit %>
      </div>
        <% end %>
      </div>
    </div>
  </div>
  

</body>
</html>