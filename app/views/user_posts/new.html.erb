<div class="col-lg-8">
  <div class="container">
    <div class="row justify-content-center align-items-center" style="height: 100vh;">
      <div class="col-lg-4">
        <%= form_with(model: @post, url: user_posts_path, html: { class: "justify-content-center" }) do |form| %>
          <div class="form-group">
            <%= form.label t('.add_photos') %>
            <%= form.file_field :images, multiple: true,  class: "form-control", value: "test" %>
          </div>
          <div class="form-group">
            <%= form.label t('.add_description') %>
            <%= form.text_area :body, class: "form-control" %>
          </div>
          <div class="form-group">
            <%= form.submit controller.action_name = t('.create_button'), class: "btn btn-primary" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
